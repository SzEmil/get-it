export enum Endpoints {
  p24Integration = '/api/p24',
}

export enum Routes {
  home = '/',
  order = '/order',
  checkout = '/order/checkout',
  privacyPolicyAndCookies = 'privacy-policy',
  termsOfService = 'terms-of-services',
  signIn = '',
  offer = '/offer',
  myCourses = '/my-courses',
  profile = '/profile',
  contact = "/contact"
}

export enum SocialLinks {
  tiktok = '/',
  facebook = '/',
}
